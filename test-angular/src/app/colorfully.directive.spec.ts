import { ColorfullyDirective } from './colorfully.directive';

describe('ColorfullyDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfullyDirective();
    expect(directive).toBeTruthy();
  });
});
